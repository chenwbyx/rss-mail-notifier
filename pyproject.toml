[project]
name = "rss-mail-notifier"
version = "1.0.0"
description = "RSS 邮件通知工具 - 基于 GitHub Actions 定时检查 RSS 源并发送邮件通知"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "feedparser>=6.0,<7.0",
]
